name: Env Demo

on: workflow_dispatch

env:
  GREETING: Hello
  NATION: Rwanda

jobs:
  env_demonstration:
    env:
      NATION: Germany
    runs-on: ubuntu-latest
    steps:
      - name: connecting to the env
        env:
          NATION: Germany
        run: |
          echo "amakuru is $GREETING in english"
          echo "the best country is $NATION"

